import { render, screen } from "@testing-library/react";
import { Input } from "../Input";

describe("Input component", () => {
  it("should render correctly", () => {
    render(<Input />);
    const input = screen.getByRole("textbox"); // Changed to "textbox" for correct role
    expect(input).toBeInTheDocument(); // Corrected the assertion method
  });
});

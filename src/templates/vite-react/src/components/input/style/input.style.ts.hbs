import styled from "@emotion/styled";
import { InputProps } from "../types/inputProps.type";
import { getPadding, getFontSize } from "../functions/data.utils";

export const StyledInput = styled.input<InputProps>`
  border: ${(props) => (!props.className ? "1px solid #e6e6e6" : "inherit")};
  border-radius: ${(props) => (!props.className ? "4px" : "inherit")};
  padding: ${(props) =>
    !props.className ? getPadding(props.inputSize) : "inherit"};
  font-size: ${(props) =>
    !props.className ? getFontSize(props.inputSize) : "inherit"};
  width: ${(props) => (!props.className ? "100%" : "inherit")};
  box-sizing: border-box;
  outline: none;
  transition:
    border-color 0.2s ease,
    box-shadow 0.2s ease;

  &:focus {
    border-color: ${(props) => (!props.className ? "#ed8822" : "inherit")};
    box-shadow: ${(props) =>
      !props.className ? "0 0 0 2px rgba(237, 136, 34, 0.2)" : "inherit"};
  }
`;
